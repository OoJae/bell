# Rust / Anchor build output
target/
**/*.rs.bk

# Keypairs — the program keypair is the upgrade authority. Never commit these.
*-keypair.json
.anchor/
test-ledger/

node_modules/
data/*.db
data/*.db-shm
data/*.db-wal
data/snapshots/
.env

.playwright-mcp/
.DS_Store
.attestor.json
test-ledger/
.filler.json
localnet/
.demo*.env
.env.local
web/.env.local

# Build output and environment, closed off before the repo goes public.
.next/
*.tsbuildinfo
.env.*
.railway/

# Demo-funds faucet key. Hosted only as BELL_KEY_FAUCET; the file lives outside the repo.
.faucet.json
*faucet*.json
